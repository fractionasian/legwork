.claude/
.DS_Store
.playwright-mcp/
node_modules/

# Generated map tiles now live in the legwork-tiles repo (served via its Pages site).
# Build pipeline: github.com/fractionasian/legwork-tiles/.github/workflows/build-tiles.yml
# data/cities.json (the source config) stays tracked here.
data/tiles/
data/manifest.json
# Suburb polygons are generated by scripts/build-suburbs.js and published to the
# same legwork-tiles Pages site, at /suburbs/<city>.json.
data/suburbs/

# lint gate temp bundle
.eslint-bundle.js
